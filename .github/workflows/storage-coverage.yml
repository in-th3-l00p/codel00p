name: storage coverage

on:
  push:
    branches: [main]
  pull_request:

# Run all JavaScript actions on Node 24 (Node 20 is being phased out).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  storage-coverage:
    name: Storage coverage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: core

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Check storage line coverage
        run: |
          cd core
          cargo llvm-cov -p codel00p-storage --features sqlite --summary-only --fail-under-lines 100
