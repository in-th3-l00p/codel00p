import { ProductShell } from "@codel00p/ui";

export default function CloudPage() {
  return (
    <ProductShell
      eyebrow="Cloud platform"
      title="Organization memory"
      description="Manage teams, projects, provider policy, and shared knowledge from one control surface."
    />
  );
}
