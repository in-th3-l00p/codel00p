# Packages

Shared TypeScript packages used by codel00p applications.

- `ui`: reusable React UI primitives.
- `protocol-ts`: TypeScript protocol contracts.
- `sdk`: client SDK for apps.
- `config`: shared configuration package.
