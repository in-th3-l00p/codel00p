# CLI Provider Policy Preset

## Goal

Let CLI agent runs select a built-in provider policy preset by stable preset ID.

## Scope

- [x] Add failing CLI tests for preset ID validation and policy enforcement.
- [x] Add `--provider-policy-preset <id>` to `agent run` and `agent resume`.
- [x] Update CLI/provider docs and roadmap/backlog notes.
- [x] Run focused CLI/provider tests and full `pnpm verify`.
- [x] Commit and push without coauthor trailers.
