# Memory Quality CLI And MCP

## Goal

Expose the core low-quality memory review queue through scriptable CLI and MCP
surfaces.

## Scope

- [x] Add failing CLI coverage for `memory quality`.
- [x] Add failing MCP coverage for `memory_quality`.
- [x] Implement `memory quality --max-score <0-100> --limit <n> [--json]`.
- [x] Implement the `memory_quality` MCP tool with matching arguments.
- [x] Update CLI help and docs.
